{
  "scripts": {
    "start:user-service": "npm run --prefix user-service start",
    "start:user-activity-history-service": "npm run --prefix user-activity-history-service start",
    "start": "cross-env FORCE_COLOR=1 npm-run-all -l -p start:*"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "wait-on": "^7.0.1"
  }
}